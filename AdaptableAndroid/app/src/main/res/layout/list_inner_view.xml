<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" 
    android:layout_height="wrap_content">
    <CheckBox
        android:id="@+id/checkBox1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:text="testing the length of the checkbox because now i am frustrated at the size that i am seeing for eaach checkbox and if it goes past a boundary what will it do to wrap around because it is very peculiar"
        android:textColor="#000"
        android:lines="1"
        android:ellipsize="end"
        android:layout_alignBottom="@+id/infoButton"
        android:layout_toLeftOf="@+id/infoButton"
        android:layout_toStartOf="@+id/infoButton"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/infoButton"
        android:id="@+id/infoButton"
        android:clickable="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

</RelativeLayout>
